# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Logs / temp
*.log
/scratch/
/tmp/

# Node (only if a contributor adds tooling locally — runtime stays build-free)
node_modules/
package-lock.json

# Local environment
.env
.env.local

# ML reference dataset (captures + generated counterparts; huge, never commit)
dataset/

# Private deploy playbook (contains the production server address — never commit)
deploy.md

# Meshy working files (prompts/reference; not part of the shipped game)
meshy/

# Third-party Blender MCP addon (install in Blender; get it from
# github.com/ahujasid/blender-mcp). Not bundled in this repo.
tools/blender_mcp_addon.py

# Temp asset staging (model optimization/preview; never commit)
.asset-tmp/

# Local TTS voice workspace (engines, model weights, takes; ~12 GB — the
# curated .ogg results are copied into sounds/ and committed from there)
voice-generator/

# Python bytecode cache (tools/)
__pycache__/
